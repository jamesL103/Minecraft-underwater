num radius = randomInt(2) + 1;

for (num x = -radius; x < radius; x = x + 1) {
	for (num z = -radius; z < radius; z = z + 1) {
		if(pow(x,2) + pow(z,2) < pow(radius,2)) {
			block(x,0,z, "underwater:coral_garden");
		}
	}
}